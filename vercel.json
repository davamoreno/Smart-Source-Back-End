{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "build": {
      "command": "npm run build && php artisan optimize:clear",
      "env": {
        "NODE_ENV": "production"
      } 
    },
      "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.php"
      }
    ]
}